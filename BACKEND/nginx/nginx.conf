events {
    worker_connections 1024;
}

http {
    # Allow large file uploads (up to 500MB)
    client_max_body_size 500M;
    
    server {
        listen 8090;
        
        # Auth Service
        location /auth/ {
            proxy_pass http://auth:8888/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-URI $request_uri;
            proxy_set_header X-Original-Path /auth;
        }
        
        # Admin Service
        location /admin/ {
            proxy_pass http://be_admin:8888/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-URI $request_uri;
            proxy_set_header X-Original-Path /admin;
            
            # Extended timeouts for long-running operations
            proxy_read_timeout 300s;
            proxy_connect_timeout 300s;
            proxy_send_timeout 300s;
        }
        
        # Chatbot Service
        location /chat/ {
            proxy_pass http://be_chatbot:8888/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-URI $request_uri;
            proxy_set_header X-Original-Path /chat;
            
            # Extended timeouts for streaming responses
            proxy_read_timeout 300s;
            proxy_connect_timeout 300s;
            proxy_send_timeout 300s;
            
            # SSE/Streaming support
            proxy_buffering off;
            proxy_cache off;
            proxy_set_header Connection '';
            proxy_http_version 1.1;
            chunked_transfer_encoding on;
        }
        
        # Preprocess Service
        location /preprocess/ {
            proxy_pass http://be_preprocess:8888/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-URI $request_uri;
            proxy_set_header X-Original-Path /preprocess;
            
            # Extended timeouts for long-running data processing operations
            proxy_read_timeout 3600s;  # 1 hour - for long-running processing
            proxy_connect_timeout 600s;  # 10 minutes - for connection establishment
            proxy_send_timeout 600s;  # 10 minutes - for sending data
            client_max_body_size 500M;
            
            # Keep connection alive for long operations
            proxy_http_version 1.1;
            proxy_set_header Connection '';
        }
        
        # ML Service
        location /ml/ {
            proxy_pass http://ml:8888/;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_set_header X-Original-URI $request_uri;
            proxy_set_header X-Original-Path /ml;
            
            # Extended timeouts for ML training and large file uploads
            proxy_read_timeout 1800s;  # 30 minutes
            proxy_connect_timeout 600s;  # 10 minutes
            proxy_send_timeout 600s;  # 10 minutes
            client_max_body_size 500M;
        }
    }
} 